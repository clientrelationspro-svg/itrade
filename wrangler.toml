name = "ai-trade-platform-api"
type = "python-worker"
account_id = ""  # 你的 Cloudflare Account ID
workers_dev = true
compatibility_date = "2024-06-14"

# 环境变量
[vars]
SILICONFLOW_API_KEY = "sk-tkmklxuruwxdqbuqzpcqucbzbcjwomvxuzcththgmwjtpxdz"
AI_MODEL_VISION = "Qwen/Qwen3-VL-8B-Instruct"

# D1 数据库（Cloudflare 的 SQLite）
[[d1_databases]]
binding = "DB"
database_name = "ai-trade-db"
database_id = ""  # 部署时创建

# 静态文件（上传目录）
[site]
bucket = "./uploads"
public = "."

# 构建配置
[build]
command = "pip install -r requirements.txt -t . && python -m compileall ."
upload_format = "python-worker"
